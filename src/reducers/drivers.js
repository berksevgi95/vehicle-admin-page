import { Drivers } from "../@fake-db";

const VehiclesState = (state = Drivers, action) => {
    switch (action.type) {

        default:
            return state
    }
}

export default VehiclesState